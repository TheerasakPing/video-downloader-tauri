# รายชื่อเว็บไซต์ที่รองรับและขั้นตอนการดาวน์โหลดวิดีโอ (Tauri Native)

จากการวิเคราะห์ระบบ Backend (Rust) ของแอปพลิเคชันนี้ พบว่าแอปถูกพัฒนาขึ้นมาเพื่อ **เจาะฝ่าการป้องกันและดาวน์โหลดจาก 4 แหล่งวิดีโอหลัก** โดยตรง ซึ่งต่างจากการใช้ปลั๊กอินครอบจักรวาลทั่วไป ดังนี้:

## 🌐 เว็บไซต์ที่รองรับการเจาะระบบดาวน์โหลด natively (Supported Platforms)

1. **rongyok.com / thongyok.com** (ซีรีส์และละครจีน-เอเชีย)
   - ใช้หลักการแยกโครงสร้าง API ของเว็บไซต์ (`RongyokParser`) และดึงหาลิงก์วิดีโอต้นฉบับออกมาพร้อมจำนวนตอนทั้งหมดได้อัตโนมัติ

2. **xn--82c7abb4jua0l.com (บ้านจีน.com)**
   - ระบบของเว็บนี้มีชั้นการดักจับ (Obfuscation) มียูสเซอร์ `BaanJeenParser` ซึ่งหากการดึงลิงก์รูปแบบ API และ HTML ธรรมดาแบบ Static ไม่สำเร็จ ตัวแอป **มีฟังก์ชัน Hybrid (ChromeVideoDetector)** เพื่อสั่งเปิดบราวเซอร์ Google Chrome/Edge ขึ้นมาดักจับ `.m3u8` จาก Video Player และ iFrame โดยเฉพาะ
3. **51cg1.com / 51cg3.com (ระบบ Titan)**
   - รองรับตัวเล่นวิดีโอรหัส Titan (`TitanParser`) และการสตรีมมิ่งเซิร์ฟเวอร์แบบเฉพาะ ดึงรูปภาพปกและเพลย์ลิสต์ได้โดยตรง

4. **Direct Video Links (แอดเดรสวิดีโอโดยตรง)**
   - หากนำลิงก์ประเภทสตรีมมิ่ง `.m3u8`, วิดีโอสด `.ts` หรือไฟล์เดี่ยว `.mp4` มาวาง ระบบก็สามารถรวมไฟล์เป็น 1 ตอนแล้วทำการดาวน์โหลดได้ทันที

---

## ⚙️ ขั้นตอนการดาวน์โหลด (App Download Flow)

กระบวนการการทำงานตั้งแต่ผู้ใช้ส่งลิงก์จนจบกระบวนการ จะถูกรันด้วยเครื่องมือที่เขียนขึ้นใหม่บน Rust เพื่อรีดประสิทธิภาพสูงสุด และลดการกินสเปคเครื่อง:

### 1. การรับข้อมูลและตรวจสอบ (Input Validation & Smart Routing)

- ระบบรับลิงก์ (URL) จากผู้ใช้งาน
- แอปจะวิเคราะห์โดเมนเนมของลิงก์ว่าตรงกับ `Parser` ตัวไหน (เช่น 51cg1 -> TitanParser, บ้านจีน -> BaanJeenParser)
- สำหรับ Direct Video (`.mp4`, `.m3u8`) จะข้ามเข้าสู่ระบบดาวน์โหลดเป็นไฟล์เดี่ยวทันที

### 2. การแยกรายละเอียดรายตอน (Series Fetching & Bypassing)

- Rust Backend จะเข้าไปดึงรายชื่อ "ตอน" ทั้งหมดของซีรีส์ (Episodes) รวมถึงโหลดรูปภาพปกโปสเตอร์ แปลงเป็น `base64` ส่งกลับมาโชว์ที่หน้าแอปโดยไม่กินเน็ต
- หากเว็บมีระบบป้องกัน ฝั่ง **BaanJeen** จะสับสวิตช์ไปเปิด `Chrome/Edge Detector` เบื้องหลัง เพื่อดมกลิ่น(sniff) แพ็คเกจ `.m3u8` จากการโหลด iFrame วิดีโอ แล้วส่งเข้าแอปเอง

### 3. การประมวลผลดาวน์โหลดแบบขนาน (Concurrent Downloading)

- เมื่อเลือกว่าจะดาวน์โหลดตอนไหนบ้าง ก็เริ่มสั่ง `start_download`
- แบ็คเอนด์สามารถโหลดทีละหลายตอนพร้อมกัน (Concurrent Downloads) โดยผู้ใช้สามารถตั้งจำกัดความเร็วเน็ต (Speed Limit) ได้เอง
- มีฟังก์ชันให้ผู้ใช้ตั้งชื่อไฟล์ก่อนบันทึก (File Naming) เพื่อให้จัดเรียงง่าย เช่น โหลดมาเป็นชื่อ `เรื่องนี้_EP_1.mp4`

### 4. การจัดการระหว่างโหลด (State Management)

- แบ็คเอนด์จะเปิดพอร์ตส่งสัญญาณเข้าหน้าแอปแบบ Real-time (ผ่าน Event Emit) เพื่ออัปเดตเปอร์เซ็นต์ หรือให้ผู้ใช้สามารถบังคับสั่ง **Pause (หยุดชั่วคราว)**, **Resume (โหลดต่อ)** และ **Cancel (ยกเลิก)** ได้ตลอดเวลา

### 5. กระบวนการรวมไฟล์ (Merge with FFmpeg)

- ถ้าเว็บไซต์นั้นสตรีมข้อมูลแยกกัน (คลื่นภาพ คลื่นเสียง ท่อนวิดีโอ) และกดให้ `auto_merge`
- หลังจากโหลดครบ ระบบจะตรวจเช็คว่าในเครื่องผู้ใช้มีโปรแกรม `ffmpeg` หรือไม่
- หากมี จะเร่งประสานท่อนวิดีโอ (Merging) เข้าด้วยกันเป็นไฟล์เดียว (`.mp4`) เสร็จแล้วระบบก็เคลียร์ไฟล์ย่อยขยะที่แตกไว้ทิ้งทั้งหมดไป

### 6. การสรุปและเปิดโฟลเดอร์รอดู (Completion & Listing)

- ผู้ใช้งานสามารถให้แอปรวบรวมรายชื่อไฟล์วิดีโอที่โหลดเสร็จแล้วมาแสดง
- สามารถกดที่คำสั่ง (Command) เพื่อกด Play เปิดวิดีโอดู, สั่ง Delete หรือเปิดโฟลเดอร์ใน Windows Explorer / Finder (MacOS) ขึ้นมาโดยตรงจากตัวแอปพลิเคชัน
